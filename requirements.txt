fastapi>=0.115.0
uvicorn[standard]>=0.30.6
sqlalchemy>=2.0.35
python-jose[cryptography]>=3.3.0
bcrypt>=4.0.1
python-multipart>=0.0.9
pydantic[email]>=2.9.2
python-dotenv>=1.0.1
pypdf>=4.3.1

# Optional: only needed if LLM_PROVIDER=openai
openai>=1.51.0

# Optional: only needed for LangChain-based pipeline extensions
# (not required for the app to run as shipped)
# langchain>=0.3.1
